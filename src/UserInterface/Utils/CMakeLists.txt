ADD_LIBRARY(Utils 
  ProgressBar.cpp
)

TARGET_LINK_LIBRARIES(Utils PUBLIC
  ${VTK_LIBRARIES}
  Qt${QT_VERSION_MAJOR}::Widgets
  Qt${QT_VERSION_MAJOR}::OpenGLWidgets 
  Qt${QT_VERSION_MAJOR}::Core
  Qt${QT_VERSION_MAJOR}::Gui
  Qt${QT_VERSION_MAJOR}::Xml
  Defaults
  Widgets
  TreeStructure
  Rendering
  TreeItems
  Shared
  GeometryCore
  Model
)


TARGET_INCLUDE_DIRECTORIES(Utils PUBLIC
    ${PRJ_SOURCE_DIR}/src/Model/GeometryCore/Geometry
    ${PRJ_SOURCE_DIR}/src/Model/GeometryCore/Geometry
    ${PRJ_SOURCE_DIR}/src/UserInterface/Utils
    ${PRJ_SOURCE_DIR}/src/UserInterface/Defaults
    ${PRJ_SOURCE_DIR}/src/UserInterface/TreeStructure/TreeItems
    ${PRJ_SOURCE_DIR}/src/UserInterface/Utils/QVTKProgressBar
    )

# Autogenerated uic files
if(WIN32)
  TARGET_INCLUDE_DIRECTORIES(Utils INTERFACE 
    ${CMAKE_CURRENT_BINARY_DIR}/Utils_autogen/include_Release
  )
else()
  TARGET_INCLUDE_DIRECTORIES(Utils INTERFACE 
    ${CMAKE_CURRENT_BINARY_DIR}/Utils_autogen/include
  )
endif()